\begin{tikzpicture}[node distance = 4.0cm, on grid, auto]
    
         % DFA states where states are modified
         \node[state, initial] (q0) {$q_0$};
         \node[state, below right of=q0] (q1) {$q_1$};
         \node[state, above right of=q1] (q2) {$q_2$};
         \node[state, accepting, above right of=q0] (q3) {$q_3$};
    
         %Transitions which are unchanged
         \path[->]
         (q0) edge[loop above] node {0} ()
              edge[above]      node {1} (q1)
              edge[bend left]  node {2} (q2)
              edge[bend left]  node {3} (q3)
    
         (q1) edge[loop above] node {0} ()
              edge[above]      node {1} (q2)
              edge[bend left]  node {2} (q3)
              edge[bend left]  node {3} (q0)
         
         (q2) edge[loop above] node {0} ()
              edge[below]      node {1} (q3)
              edge[bend left]  node {2} (q0)
              edge[bend left]  node {3} (q1)
         
         (q3) edge[loop above] node {0} ()
              edge[bend left]  node {1} (q0)
              edge[bend left]  node {2} (q1)
              edge[below]      node {3} (q2);
\end{tikzpicture}